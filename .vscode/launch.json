{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "chrome",
      "request": "launch",
      "name": "Launch app",
      "url": "http://localhost:5173/expense-split-manager/",
      "webRoot": "${workspaceFolder}",
      "preLaunchTask": "Start Vite dev server"
    }
  ]
}
